const config = {
  screens: {
    EnterNewPasswordToReset: {
      path: 'EnterNewPasswordToReset',
    },
  },
};

const linking = {
  prefixes: ['apartner://app'],
  config,
};

export default linking;
